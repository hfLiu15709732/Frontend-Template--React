# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# 项目所属依赖项(不上传)，克隆后yarn一下即可
/node_modules
/.pnp
.pnp.js

# 本地测试项文件夹(不上传)
/coverage

# 构建后产品文件夹(不上传)
/build

# 本地实用工具集(不上传)
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
